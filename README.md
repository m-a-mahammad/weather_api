# 🌤️ Weather App with Redis Caching

تطبيق ويب بسيط يعرض حالة الطقس لمدينة معينة باستخدام OpenWeather API، مع تخزين مؤقت للبيانات باستخدام Redis لتحسين الأداء وتقليل عدد الطلبات الخارجية.

## 🚀 الميزات

- 🔍 عرض حالة الطقس لأي مدينة
- ⚡ استخدام Redis لتخزين البيانات مؤقتًا لمدة ساعة
- 🖼️ واجهة مستخدم بسيطة باستخدام EJS
- 🌐 دعم الوحدات (مئوية/فهرنهايت) عبر query params

## 🛠️ المتطلبات

- Node.js >= 18
- Redis Server
- حساب في OpenWeather للحصول على API Key

## 📦 التثبيت

```bash
npm install
```
## 🔐 إعداد البيئة

أنشئ ملف `.env` في جذر المشروع يحتوي على:

```env
OPEN_WEATHER_SECRET=your_openweather_api_key
C_TEMP=metric
```
## ▶️ التشغيل

```bash
npm run dev
```

السيرفر يعمل على: [http://localhost:3000](http://localhost:3000/)

## 🌐 نقاط الوصول
### /weather

يعرض حالة الطقس لمدينة معينة
- يمكن تمرير اسم المدينة عبر `?city=`
- يمكن تحديد الوحدة عبر `?units=metric` أو `imperial`

مثال:

```bash
/weather?city=Giza&units=metric
```
## 🧠 التخزين المؤقت (Redis)

- يتم تخزين نتيجة كل طلب في Redis لمدة 3600 ثانية (ساعة واحدة)
- المفتاح المستخدم هو: `weather?q=<city>&units=<unit>`
- إذا كانت البيانات موجودة في Redis، يتم عرضها مباشرة بدون طلب جديد
## 📂 هيكل المشروع

```
├── src/
├── views/
│	└── home.ejs         # واجهة المستخدم 
├── index.ts                 # الكود الرئيسي للسيرفر
├── .env                     # إعدادات البيئة
├── package.json
└── README.md
```

## 🖼️ واجهة المستخدم

- نموذج إدخال لاسم المدينة
- عرض صورة الطقس، درجة الحرارة، الرطوبة، سرعة الرياح، والوصف
- تصميم بسيط وحديث باستخدام CSS داخلي
